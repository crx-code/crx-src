--TEST--
MySQL PDO crxinfo() output
--EXTENSIONS--
pdo_mysql
--SKIPIF--
<?crx
require_once(__DIR__ . DIRECTORY_SEPARATOR . 'mysql_pdo_test.inc');
MySQLPDOTest::skip();
$db = MySQLPDOTest::factory();
?>
--FILE--
<?crx
    require_once(__DIR__ . DIRECTORY_SEPARATOR . 'mysql_pdo_test.inc');
    $db = MySQLPDOTest::factory();

    ob_start();
    crxinfo();
    $tmp = ob_get_contents();
    ob_end_clean();

    /*	PDO Driver for MySQL, client library version => 6.0.3-alpha	*/
    $reg = 'Client API version.*' . preg_quote($db->getAttribute(PDO::ATTR_CLIENT_VERSION), '/');

    if (!preg_match("/$reg/", $tmp)) {
        printf("[001] Cannot find MySQL PDO driver line in crxinfo() output\n");
    }

    print "done!";
?>
--EXPECT--
done!
