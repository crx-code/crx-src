--TEST--
realpath() with relative directory
--FILE--
<?crx

var_dump(realpath('.') == realpath(getcwd()));
chdir('..');
var_dump(realpath('.') == realpath(getcwd()));

?>
--EXPECT--
bool(true)
bool(true)
