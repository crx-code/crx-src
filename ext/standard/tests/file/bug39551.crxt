--TEST--
Bug #39551 (Segfault with stream_bucket_new in user filter)
--FILE--
<?crx

$bucket = stream_bucket_new(fopen('crx://temp', 'w+'), '');

class bucketFilter extends crx_user_filter {
    public function filter($in, $out, &$consumed, $closing ): int {

        $bucket = stream_bucket_new(fopen('crx://temp', 'w+'), '');
        stream_bucket_append($out, $bucket);
        return PSFS_PASS_ON;
    }
}

stream_filter_register('bucketfault', 'bucketFilter');
stream_filter_append($s = fopen('crx://temp', 'r+'), 'bucketfault');
stream_get_contents($s);

echo "Done\n";
?>
--EXPECT--
Done
