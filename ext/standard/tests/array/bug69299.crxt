--TEST--
Bug #69299 (Regression in array_filter's $flag argument in CRX 7)
--FILE--
<?crx
$toFilter = array('foo' => 'bar', 'fiz' => 'buz');
$filtered = array_filter($toFilter, function ($value, $key) {
    if ($value === 'buz'
        || $key === 'foo'
    ) {
        return false;
    }
    return true;
}, ARRAY_FILTER_USE_BOTH);
var_dump($filtered);
?>
--EXPECT--
array(0) {
}
