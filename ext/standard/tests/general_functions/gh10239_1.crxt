--TEST--
GH-10239 (proc_close after proc_get_status always returns -1)
--SKIPIF--
<?crx
if (CRX_OS != "Linux") die("skip, only for linux");
if (getenv("SKIP_SLOW_TESTS")) die('skip slow test');
?>
--FILE--
<?crx
$p = proc_open('sleep 1', array(), $foo);
do {
    usleep(100000);
    $s = proc_get_status($p);
} while ($s['running']);
echo proc_close($p), CRX_EOL;
?>
--EXPECT--
0
