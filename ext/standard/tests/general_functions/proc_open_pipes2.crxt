--TEST--
proc_open() with no pipes
--FILE--
<?crx

$spec = array();

$crx = getenv("TEST_CRX_EXECUTABLE_ESCAPED");
$callee = escapeshellarg(__DIR__ . "/proc_open_pipes_sleep.inc");
proc_open("$crx -n $callee", $spec, $pipes);

var_dump(count($spec));
var_dump($pipes);

?>
--EXPECT--
int(0)
array(0) {
}
