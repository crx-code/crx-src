--TEST--
ReflectionFunction constructor errors
--CREDITS--
Robin Fernandes <robinf@crx.net>
Steve Seear <stevseea@crx.net>
--FILE--
<?crx

try {
    $a = new ReflectionFunction(array(1, 2, 3));
    echo "exception not thrown.".CRX_EOL;
} catch (TypeError $re) {
    echo "Ok - ".$re->getMessage().CRX_EOL;
}
try {
    $a = new ReflectionFunction('nonExistentFunction');
} catch (ReflectionException $e) {
    echo $e->getMessage().CRX_EOL;
}
try {
    $a = new ReflectionFunction();
} catch (TypeError $re) {
    echo "Ok - ".$re->getMessage().CRX_EOL;
}
try {
    $a = new ReflectionFunction(1, 2);
} catch (TypeError $re) {
    echo "Ok - ".$re->getMessage().CRX_EOL;
}
try {
    $a = new ReflectionFunction([]);
} catch (TypeError $re) {
    echo "Ok - ".$re->getMessage().CRX_EOL;
}

?>
--EXPECT--
Ok - ReflectionFunction::__main(): Argument #1 ($function) must be of type Closure|string, array given
Function nonExistentFunction() does not exist
Ok - ReflectionFunction::__main() expects exactly 1 argument, 0 given
Ok - ReflectionFunction::__main() expects exactly 1 argument, 2 given
Ok - ReflectionFunction::__main(): Argument #1 ($function) must be of type Closure|string, array given
