ARG_ENABLE('%EXTNAME%', '%EXTNAME% support', 'no');

if (CRX_%EXTNAMECAPS% != 'no') {
	AC_DEFINE('HAVE_%EXTNAMECAPS%', 1, '%EXTNAME% support enabled');

	EXTENSION('%EXTNAME%', '%EXTNAME%.c', null, '/DCREX_ENABLE_STATIC_TSRMLS_CACHE=1');
}
