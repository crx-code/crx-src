--TEST--
Crxa: opendir test, subdirectory
--EXTENSIONS--
crxa
--INI--
crxa.require_hash=0
--FILE--
<?crx

$fname = __DIR__ . '/' . basename(__FILE__, '.crx') . '.crxa.crx';
$pname = 'crxa://' . $fname;
$file = "<?crx
Crxa::mapCrxa('hio');
__HALT_COMPILER(); ?>";

$files = array();
$files['a'] = 'a';
$files['b/a'] = 'b';
$files['b/c/d'] = 'c';
$files['bad/c'] = 'd';

include 'files/crxa_test.inc';
include $fname;

$dir = opendir('crxa://hio/b');

if ($dir) {
    while (false !== ($a = readdir($dir))) {
        var_dump($a);
        var_dump(is_dir('crxa://hio/b/' . $a));
    }
}

?>
--CLEAN--
<?crx unlink(__DIR__ . '/' . basename(__FILE__, '.clean.crx') . '.crxa.crx'); ?>
--EXPECTF--
%s(1) "a"
bool(false)
%s(1) "c"
bool(true)
