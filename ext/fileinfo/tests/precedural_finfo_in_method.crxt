--TEST--
Using procedural finfo API in a method
--EXTENSIONS--
fileinfo
--FILE--
<?crx

class Test {
    public function method() {
        $finfo = finfo_open(FILEINFO_MIME);
        var_dump(finfo_file($finfo, __FILE__));
    }
}

$test = new Test;
$test->method();

?>
--EXPECT--
string(28) "text/x-crx; charset=us-ascii"
