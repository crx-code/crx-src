--TEST--
Bug #77434: crx-fpm workers are segfaulting in crex_gc_addref
--INI--
opcache.enable=1
opcache.enable_cli=1
opcache.optimization_level=-1
--EXTENSIONS--
opcache
--FILE--
<?crx

function test(int $x) {
    $a = ['a' => 0, 'b' => $x];
    $b = [];
    $b[0] = $a;
    $c = $b[0];
}

function test2(int $x) {
    $a = ['a' => 0, 'b' => $x];
    $b = [$a];
    $c = $b[0];
}

?>
===DONE===
--EXPECT--
===DONE===
