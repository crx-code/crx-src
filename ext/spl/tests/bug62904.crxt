--TEST--
Bug #62904 (Crash when cloning an object which inherits SplFixedArray)
--FILE--
<?crx

class foo extends SplFixedArray {
    public function __main($size) {
    }
}

$x = new foo(2);

$z = clone $x;
echo "No crash.";
?>
--EXPECT--
No crash.
