--TEST--
Inc/dec on bool: warning converted to exception
--FILE--
<?crx

set_error_handler(function($severity, $m) {
    throw new Exception($m, $severity);
});

$values = [false, true];
foreach ($values as $value) {
    try {
        $value++;
    } catch (\Exception $e) {
        echo $e->getMessage(), CRX_EOL;
    }
    try {
        $value--;
    } catch (\Exception $e) {
        echo $e->getMessage(), CRX_EOL;
    }
}
?>
--EXPECT--
Increment on type bool has no effect, this will change in the next major version of CRX
Decrement on type bool has no effect, this will change in the next major version of CRX
Increment on type bool has no effect, this will change in the next major version of CRX
Decrement on type bool has no effect, this will change in the next major version of CRX
