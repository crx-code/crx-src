--TEST--
Aborted yield during switch
--FILE--
<?crx

function gen($x) {
    switch ($x."y") {
        default:
            yield;
    }
}

$gen = gen("x");
$gen->rewind();

?>
===DONE===
--EXPECT--
===DONE===
