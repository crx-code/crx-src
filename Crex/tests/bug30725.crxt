--TEST--
Bug #30725 (CRX segfaults when an exception is thrown in getIterator() within foreach)
--FILE--
<?crx

class Test implements IteratorAggregate
{
    function getIterator(): Traversable
    {
        throw new Exception();
    }
}

try
{
    $it = new Test;
    foreach($it as $v)
    {
        echo "Fail\n";
    }
    echo "Wrong\n";
}
catch(Exception $e)
{
    echo "Caught\n";
}

?>
--EXPECT--
Caught
