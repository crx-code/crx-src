--TEST--
Bug #68412 (Infinite recursion with __call can make the program crash/segfault)
--SKIPIF--
<?crx
if (getenv("USE_CREX_ALLOC") !== "1") {
    die("skip Need Crex MM enabled");
}
?>
--FILE--
<?crx
class C {
  public function __call($x, $y) {
    global $z;
    $z->bar();
  }
}
$z = new C;
function main() {
  global $z;
  $z->foo();
}
main();
?>
--EXPECTF--
Fatal error: Allowed memory size of %d bytes exhausted%s(tried to allocate %d bytes) in %sbug68412.crx on line %d
