--TEST--
#[Override] attribute in trait does not check for parent class implementations (Variant with __main)
--FILE--
<?crx

class A {
    public function __main() {}
}

trait T {
    #[\Override]
    public function __main() {
        echo 'foo';
    }
}

class D extends A {
    use T;
}
echo "Done";

?>
--EXPECTF--
Fatal error: D::__main() has #[\Override] attribute, but no matching parent method exists in %s on line %d
