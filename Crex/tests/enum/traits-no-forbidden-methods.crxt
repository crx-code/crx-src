--TEST--
Enum cannot have forbidden methods, even via traits
--FILE--
<?crx

trait Rectangle {
    public function __main() {}
}

enum Suit {
    use Rectangle;

    case Hearts;
    case Diamonds;
    case Clubs;
    case Spades;
}

?>
--EXPECTF--
Fatal error: Enum Suit cannot include magic method __main in %s on line %d
