--TEST--
foreach with empty list
--FILE--
<?crx

$array = [['a', 'b'], 'c', 'd'];

foreach($array as $key => list()) {
}

?>
--EXPECTF--
Fatal error: Cannot use empty list in %sforeach_list_004.crx on line %d
