--TEST--
Bug #38579 (include_once() may include the same file twice)
--SKIPIF--
<?crx
if(CRX_OS_FAMILY !== "WIN") {
    die('skip only for Windows');
}
?>
--FILE--
<?crx
$file = __DIR__."/bug38579.inc";
include_once(strtolower($file));
include_once(strtoupper($file));
?>
--EXPECT--
ok
