--TEST--
ZE2 a class cannot extend an interface
--FILE--
<?crx

interface Test
{
    function show();
}

class Tester extends Test
{
    function show() {
        echo __METHOD__ . "\n";
    }
}

$o = new Tester;
$o->show();

?>
===DONE===
--EXPECTF--
Fatal error: Class Tester cannot extend interface Test in %sinterface_and_extends.crx on line %d
