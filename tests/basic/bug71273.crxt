--TEST--
Bug #71273 A wrong ext directory setup in crx.ini leads to crash
--FILE--
<?crx
    /* NOTE this file is required to be encoded in iso-8859-1 */

    $cmd = getenv('TEST_CRX_EXECUTABLE_ESCAPED') . " -n -d html_errors=on -d extension_dir=a/é/w -d extension=crx_kartoffelbrei.dll -v 2>&1";
    $out = shell_exec($cmd);

    var_dump(preg_match(",.+a[\\/].+[\\/]w.crx_kartoffelbrei.dll.+,s", $out));
?>
--EXPECT--
int(1)
